/* tslint:disable:no-unused-variable */
import {OrderSlotsPipe} from "./order-slots.pipe";

describe('OrderSlotsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderSlotsPipe();
    expect(pipe).toBeTruthy();
  });
});
